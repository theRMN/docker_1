#### Для зaпуска проэкта "docker-drf" нужно выполнить следующие команды:
###### docker build -t drf .
###### docker run -p 8000:8000 -d drf 
#### api будет доступно по адресу http://localhost:8000/api/v1/
